{
  "build": {
    "buildCommand": "echo building",
    "watchPatterns": [
      "src",
      "requirements.txt",
      "Dockerfile"
    ]
  },
  "deploy": {
    "startCommand": "python -m uvicorn src.api:app --host 0.0.0.0 --port 8080",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}